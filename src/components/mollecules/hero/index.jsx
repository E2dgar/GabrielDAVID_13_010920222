import HeroContent from '../heroContent';
import './index.css';

export const Hero = () => {
    return (
        <div className="hero">
            <HeroContent />
        </div>
    );
};

export default Hero;
